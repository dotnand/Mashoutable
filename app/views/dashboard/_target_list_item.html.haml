.span-15.box
  .left.list
    %span.number.left.clear= "#{item_number}."
    %span.list-item
      %span
        Add a
      = succeed "?" do
        %span.orange Target
      (or
      = "#{item_number + 1})"
  .content-bubble.span-6.left.clear
    .bubble-top
    .bubble-body
      %p Pick your target audience
    .bubble-bottom
  = render 'target'
  %hr.space/
  = render 'media_source'

